name: Update Social Content

on:
  workflow_dispatch:
  schedule:
    - cron: "17 4 * * *"

permissions:
  contents: write

jobs:
  update-content:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Refresh reels and streams data
        run: |
          python3 scripts/content/update_events.py
          python3 scripts/content/update_reels.py
          python3 scripts/content/update_streams.py
          npm run build

      - name: Commit changes if needed
        run: |
          if git diff --quiet -- src/data/reels.json src/data/streams.json src/data/events.json public/reel-thumbs; then
            echo "No content changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/data/reels.json src/data/streams.json src/data/events.json public/reel-thumbs
          git commit -m "chore: update social content"
          git push
